// Root layout — will be fully implemented in the mobile development phase.
// Placeholder to satisfy Expo Router's requirement for _layout at root.
import { Slot } from 'expo-router';

export default function RootLayout() {
  return <Slot />;
}
