FROM node:20-alpine

RUN npm install -g pnpm

WORKDIR /app

COPY package.json ./

# Install ALL deps including devDependencies (Jest, supertest)
RUN pnpm install --no-frozen-lockfile

COPY src ./src
COPY tests ./tests
COPY scripts ./scripts

CMD ["pnpm", "test", "--runInBand", "--forceExit"]
